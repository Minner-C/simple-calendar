# 简单日历 (SimpleCalendar)

一款轻量级 Windows 日历应用，完美替换系统任务栏时钟。

## ✨ 特性

- 🕐 **任务栏时钟替换** - 自动替换 Windows 11 系统时钟
- 📅 **农历显示** - 实时显示中国农历日期
- 🎉 **节假日提醒** - 内置 2025-2026 年中国法定节假日
- 🖱️ **双击弹出日历** - 点击时钟即可查看完整月历
- 🎯 **零依赖安装** - 无需安装任何额外组件，开箱即用
- 💻 **纯 C# 实现** - 安全、稳定、易维护

## 📦 安装方法

### 方式一：直接运行（推荐）

1. 下载最新发布版本 `SimpleCalendar-v1.0.0.zip`
2. 解压到任意目录
3. 双击 `SimpleCalendar.exe` 即可运行

**就是这么简单！无需安装 Visual Studio，无需编译，无需任何依赖！**

程序会自动：
- ✅ 尝试加载 ClockSubclass.dll 替换系统时钟
- ✅ 如果 DLL 不存在，使用浮动时钟方案
- ✅ 显示农历和节假日信息
- ✅ 提供日历弹窗功能

### 方式二：从源码构建

如果你需要从源码构建：

```bash
# 前提条件：安装 .NET 8 SDK
# 下载地址：https://dotnet.microsoft.com/download/dotnet/8.0

cd SimpleCalendar.Desktop
dotnet build
dotnet run
```

## 🚀 使用说明

### 启动程序

双击运行后，程序会：
1. ✅ 自动在系统托盘创建图标
2. ✅ 自动尝试替换系统时钟
3. ✅ 显示浮动时钟窗口（包含公历和农历）

### 查看日历

- **方法 1**：双击系统托盘图标
- **方法 2**：右键托盘图标 → "打开日历"
- **方法 3**：点击浮动时钟（如果支持）

### 退出程序

右键系统托盘图标 → "退出"

## 🔧 技术实现

### 核心技术

我们采用**纯 C# 窗口子类化**技术，无需外部 DLL：

1. **窗口查找** - 使用 Win32 API 查找系统时钟窗口 (`TrayClockWClass`)
2. **窗口子类化** - 通过 `SetWindowLongPtr` 替换窗口过程
3. **自定义绘制** - 拦截 `WM_PAINT` 消息，使用 GDI 绘制时间和日期
4. **双缓冲** - 避免闪烁，提供流畅的视觉体验

### 与优效日历对比

| 特性 | 优效日历 | 简单日历 |
|------|----------|----------|
| 技术实现 | C++ DLL + Hook | 纯 C# 窗口子类化 |
| 需要编译 | 是 | 否 |
| 外部依赖 | 多个 DLL | 无 |
| 安装复杂度 | 需要安装包 | 直接运行 |
| 功能丰富度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 安全性 | 可能被杀毒软件误报 | 完全安全 |

### 为什么选择纯 C# 方案？

✅ **用户友好** - 一键运行，无需任何配置  
✅ **易于分发** - 单个 EXE 文件即可  
✅ **跨版本兼容** - .NET 8 支持所有现代 Windows  
✅ **代码透明** - 全部开源，无隐藏逻辑  
✅ **维护简单** - C# 比 C++ 更易维护和调试  

## 📁 项目结构

```
SimpleCalendar.Desktop/
├── App.xaml.cs                  # 应用程序入口
├── Helpers/
│   ├── SystemClockReplacer.cs   # ⭐ 核心：系统时钟替换器
│   ├── NativeMethods.cs         # Win32 API 封装
│   └── ClockHookManager.cs      # Hook 管理器（备用）
├── Windows/
│   ├── TaskbarClockWindow.xaml  # 浮动时钟窗口
│   └── CalendarPopupWindow.xaml # 日历弹窗
├── Data/
│   ├── LunarCalendar.cs         # 农历转换算法
│   └── HolidayData.cs           # 节假日数据
└── ...
```

## 🐛 常见问题

### Q: 系统时钟没有被替换？

A: 可能原因：
1. Windows 11 24H2 限制了某些窗口操作
2. 系统权限不足（尝试以管理员身份运行）
3. 其他软件占用了时钟窗口

解决方案：
- 程序会自动显示浮动时钟作为备用
- 浮动时钟会覆盖在系统时钟位置

### Q: 时钟显示不准确？

A: 检查系统时区设置是否正确。程序使用系统时间，不会自行计算。

### Q: 如何开机自启？

A: 将快捷方式添加到启动文件夹：
```
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
```

### Q: 会被杀毒软件误报吗？

A: 纯 C# 实现不会被误报。如果使用 C++ DLL 方案，可能需要添加信任。

## 📝 开发计划

- [ ] 添加更多主题样式
- [ ] 支持自定义字体和颜色
- [ ] 添加秒数显示选项
- [ ] 支持多显示器
- [ ] 添加天气显示
- [ ] 优化性能，降低 CPU 占用

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 👥 作者

SimpleCalendar Team

---

**最后更新**: 2026-05-27  
**版本**: v1.0.0
